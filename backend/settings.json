{
  "orchestrator": {
    "model_id": "openrouter/anthropic/claude-3.5-haiku",
    "api_base": "https://openrouter.ai/api/v1",
    "system_prompt": "## Show Context:\n{{{SHOW_CONTEXT}}}\n\n## Available Macro Variables:\n{{{VARIABLE_DEFINITIONS}}}\n\n## Current System State:\n{{{CURRENT_STATE}}}\n\n## Task:\nTranslate the following director request into a JSON object matching the macro variable fields above.\n\n## Rules:\n1. Use the 'final_answer' tool to return the JSON macro object — this is mandatory.\n2. Use MCP memory tools (search_nodes/create_entities/add_observations) when the request is contextual or references past state. Treat the Knowledge Graph as your long-term memory for director preferences and show motifs.\n3. Respect the Show Context (genres, core vibes) when making artistic choices.\n4. All numeric values must stay within their specified [min, max] ranges.\n5. Include a 'duration' key (float, seconds) for the transition time.",
    "max_steps": 10,
    "temperature": 0.4,
    "max_tokens": 512,
    "tick_rate": 60,
    "default_transition_duration": 4.0,
    "min_transition_duration": 0.5
  }
}